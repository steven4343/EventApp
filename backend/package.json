{
  "name": "cuz-events-backend",
  "version": "1.0.0",
  "main": "server.ts",
  "scripts": {
    "start": "ts-node server.ts",
    "dev": "ts-node-dev --respawn server.ts",
    "seed": "ts-node seed.ts",
    "seed-images": "ts-node update-images.ts",
    "migrate": "ts-node -e \"const {database} = require('./database'); database.initialize().then(() => { console.log('Migration complete'); process.exit(0); }).catch(e => { console.error(e); process.exit(1); })\"",
    "setup": "ts-node -e \"const {database} = require('./database'); database.initialize().then(() => { console.log('Migration complete'); }).then(() => { require('./update-images'); }).catch(e => { console.error(e); process.exit(1); })\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.20.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "@types/pg": "^8.20.0",
    "@types/uuid": "^10.0.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
